const MyComponent=()=>{

    return(
        <div>
            <h1>hello</h1>
        </div>
    )
}
export default MyComponent;
//default = if we define default component then we can directly import it 